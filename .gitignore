# Builds - the script in build/ is part of the repository, what it produces is not
/build/*.zip
/build/tmp
/releases

# OSX
.DS_Store
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
Desktop.ini

# PHPStorm
.idea/

# Sublime Text
*.sublime*

# Eclipse
.buildpath
.project
.settings

# Temp files
*.tmp
*.bak
*.swp
*~.nib
*~

# composer
composer.phar
/vendor
composer.lock

# phpunit
.phpunit.result.cache

#cypress / npm
/node_modules
package-lock.json
# Cypress empties these before every run - a .gitkeep placed in them does not survive one -
# and it creates them again when it needs them, so the whole folder is output.
/tests/cypress/output
cypress.config.js
/joomla

# Github Codespaces
codespace-details.txt